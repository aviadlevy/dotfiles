# Only overrides — inherits Yazi's built-in default keymap.
# Use prepend_keymap/append_keymap so default keybindings keep flowing in on upgrades.

[mgr]
prepend_keymap = [
	{ on = "<Enter>", run = "plugin smart-enter", desc = "Enter the child directory, or open the file" },
]
